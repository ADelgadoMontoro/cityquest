{
  "name": "@cityquest/admin",
  "version": "0.1.0",
  "private": true,
  "description": "CityQuest admin panel workspace.",
  "scripts": {
    "build": "echo \"Admin build will be added in a later task.\"",
    "clean": "echo \"Nothing to clean in apps/admin yet.\"",
    "dev": "echo \"Admin app bootstrap is pending Next.js initialization.\"",
    "format": "prettier --check . --ignore-unknown",
    "format:write": "prettier --write . --ignore-unknown",
    "lint": "eslint . --max-warnings=0",
    "lint:fix": "eslint . --fix",
    "test": "echo \"Admin tests will be added when Next.js is initialized.\"",
    "test:integration": "echo \"Admin integration tests will be added when Next.js is initialized.\"",
    "test:unit": "echo \"Admin unit tests will be added when Next.js is initialized.\"",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  }
}
