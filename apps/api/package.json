{
  "name": "@cityquest/api",
  "version": "0.1.0",
  "private": true,
  "description": "CityQuest serverless API workspace.",
  "scripts": {
    "build": "echo \"API build will be added in a later task.\"",
    "clean": "echo \"Nothing to clean in apps/api yet.\"",
    "dev": "echo \"API bootstrap is pending backend initialization.\"",
    "lint": "echo \"API lint configuration will be added in a later task.\"",
    "test": "echo \"API tests will be added when the backend is initialized.\"",
    "test:integration": "echo \"API integration tests will be added when the backend is initialized.\"",
    "test:unit": "echo \"API unit tests will be added when the backend is initialized.\"",
    "typecheck": "echo \"API typecheck will be enabled after app bootstrap.\""
  }
}
