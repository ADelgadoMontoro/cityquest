{
  "name": "@cityquest/content",
  "version": "0.1.0",
  "private": true,
  "description": "Content bounded context for narrative content, hints, quizzes, and audience adaptations.",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "build": "echo \"Content context build will be added in a later task.\"",
    "clean": "echo \"Nothing to clean in packages/contexts/content yet.\"",
    "format": "prettier --check . --ignore-unknown",
    "format:write": "prettier --write . --ignore-unknown",
    "lint": "eslint . --max-warnings=0",
    "lint:fix": "eslint . --fix",
    "test": "echo \"Content tests will be added when implementation begins.\"",
    "test:integration": "echo \"Content integration tests will be added when implementation begins.\"",
    "test:unit": "echo \"Content unit tests will be added when implementation begins.\"",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  }
}
