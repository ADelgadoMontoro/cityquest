{
  "name": "@cityquest/shared-kernel",
  "version": "0.1.0",
  "private": true,
  "description": "Shared kernel primitives for CityQuest bounded contexts.",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "build": "echo \"Shared kernel build will be added in a later task.\"",
    "clean": "echo \"Nothing to clean in packages/shared-kernel yet.\"",
    "lint": "echo \"Shared kernel lint configuration will be added in a later task.\"",
    "typecheck": "echo \"Shared kernel typecheck will be enabled when shared primitives are added.\""
  }
}
