# EVO-0072 - Seed the Catedral de Jaen POI and Its First Visual Objectives in D1

## Epic

`Modelo de Datos y Gestión de Contenido`

## Objective

Insert the next real MVP content seed into `Cloudflare D1` for:

- the POI `Catedral de Jaen`
- its first visual objectives

This EVO should extend the content baseline already introduced by `EVO-0071` without broadening the content model unnecessarily, changing the route seed, or inventing editorial/gameplay content that has not been confirmed by the user.

This task is about inserting one real POI under the existing route `Jaen: Echoes of Stone` and the first objective records needed for the MVP content slice around that POI.

## Why This Task Exists

The repository already has:

- a provisioned and bound D1 database
- the initial content schema migration
- the destination `Jaen`
- the route `Jaen: Echoes of Stone`

That means the current content layer is still too shallow for route-detail endpoints or meaningful mobile route exploration.

Before implementing:

- route detail retrieval
- POI listing inside a route
- objective rendering for a real stop

the project needs at least one real POI and at least one real objective attached to it.

## Functional Context

CityQuest is a gamified cultural tourism platform.

The current MVP route is:

- `Jaen: Echoes of Stone`

This EVO should add the first stop of that route:

- `Catedral de Jaen`

and the first visual objectives associated with that stop.

This EVO must not yet add:

- `Banos Arabes`
- hints unless the user explicitly confirms them for this same slice
- unlockable content unless the user explicitly confirms it for this same slice
- progress data
- demo data

## Confirmed Business Inputs

The following non-technical values were explicitly provided by the user and should be treated as the source of truth for this EVO.

### POI

- display name: `Cathedral of Jaén`
- slug: `catedral-de-jaen`
- description: `A monumental Renaissance cathedral in the heart of Jaén, where sacred architecture, hidden stone details and local legends make the city’s history feel alive.`
- status: `published`
- latitude: `37.765738`
- longitude: `-3.789518`
- access notes: `Located in Plaza de Santa María. Some objectives may require access to the cathedral interior, so opening hours and ticket availability should be checked before visiting.`
- indoor mode: `true`
- display order: `0`

### Visual Objectives

1. `Statue of Saint Ferdinand`
   - slug: `estatua-san-fernando`
   - status: `published`
   - target type: `statue`
   - gps radius meters: `20`
   - difficulty: `easy`
   - indoor mode: `0`
   - display order: `0`

2. `Cathedral Monkey`
   - slug: `mona-catedral-jaen`
   - status: `published`
   - target type: `architectural_detail`
   - gps radius meters: `3`
   - difficulty: `hard`
   - indoor mode: `0`
   - display order: `1`

3. `Saint Catherine Choir Panel`
   - slug: `placa-santa-catalina-coro`
   - status: `published`
   - target type: `decorative_panel`
   - gps radius meters: `5`
   - difficulty: `medium`
   - indoor mode: `1`
   - display order: `2`

4. `Cathedral Organ`
   - slug: `organo-catedral-jaen`
   - status: `published`
   - target type: `architectural_detail`
   - gps radius meters: `12`
   - difficulty: `easy`
   - indoor mode: `1`
   - display order: `3`

5. `Tomb of Don Alonso Suárez`
   - slug: `tumba-don-alonso-suarez`
   - status: `published`
   - target type: `tomb`
   - gps radius meters: `3`
   - difficulty: `hard`
   - indoor mode: `1`
   - display order: `4`

## Current Repository Reality

At the time of this EVO:

- `apps/api` is the active backend workspace
- D1 is already provisioned and bound to the Worker
- the schema for POIs and visual objectives already exists
- the current schema still needs a minimal refinement if objectives must distinguish interior and exterior gameplay
- the first content seed already inserted `Jaen` and `Jaen: Echoes of Stone`
- no POIs should exist yet after `EVO-0071`

Practical interpretation:

- add data only for `pois` and `visual_objectives`
- attach the POI to the existing route seed
- keep the content scope intentionally narrow
- do not invent location, access, or objective text just to finish the task

## Technical Goal

Create the next seed migration or seed step that inserts:

1. one POI row for `Catedral de Jaen`
2. one or more visual objective rows attached to that POI

The seed must be:

- deterministic
- compatible with the existing migration-first D1 strategy
- explicit about all required business inputs
- narrow enough that `EVO-0073` can still seed `Banos Arabes` cleanly later

## Scope

This task should include:

1. choose the repository-compatible mechanism for inserting the Catedral baseline data;
2. insert one POI for `Catedral de Jaen`;
3. attach that POI to `route-jaen-echoes-of-stone`;
4. insert the first visual objectives for that POI;
5. keep identifiers, slugs, and ordering stable and explicit;
6. document the seed purpose and execution path;
7. verify that the seed works with the current D1 setup;
8. ensure existing build and test flows still pass.

## Non-Goals

This EVO must not:

- create the `Banos Arabes` POI
- create progress records
- create users
- create analytics records
- create admin-only data
- introduce unrelated schema changes beyond the minimal `visual_objectives.indoor_mode` refinement
- implement endpoints
- implement repositories
- invent final editorial text, GPS radius, or gameplay tuning without confirmation

## Recommended Data Scope

This EVO should seed only:

### pois

One row for:

- `Catedral de Jaen`

### visual_objectives

The first objective rows explicitly confirmed by the user for that POI.

If the user confirms only one viable objective, seed one.

If the user confirms multiple objectives, seed only the minimum set intended for the MVP slice.

### hints

Do not seed hints by default in this EVO.

Only include them if the user explicitly confirms that the first Catedral slice should already contain hint data and provides the actual hint text.

### unlockable_contents

Do not seed unlockable content by default in this EVO.

Only include it if the user explicitly confirms that the first Catedral slice should already contain it and provides the actual content values.

## Required Business Inputs

The following product/editorial values must be confirmed by the user before implementation is considered complete.

Do not invent them.

### POI Inputs

Required:

- POI slug
- POI display name
- POI short description or explicit `null`
- POI status
- latitude
- longitude
- access notes or explicit `null`
- indoor mode (`true` / `false`)
- display order within the route

### Visual Objective Inputs

Required for each objective:

- objective slug
- objective display title
- objective short description or explicit `null`
- objective status
- target type
- GPS radius in meters or explicit `null`
- difficulty label or explicit `null`
- display order within the POI

### Optional Inputs If This EVO Includes Them

Hints:

- hint level
- hint text
- whether the hint penalizes perfect completion

Unlockable content:

- content title
- short text or explicit `null`
- long text or explicit `null`
- content type
- status
- audio URL or explicit `null`
- image URL or explicit `null`
- display order

## Confirmed Technical Context

The following technical references are already established and should be reused:

- destination ID: `destination-jaen`
- destination slug: `jaen`
- route ID: `route-jaen-echoes-of-stone`
- route slug: `jaen-echoes-of-stone`

Recommended POI naming direction:

```txt
id: poi-jaen-cathedral
slug: jaen-cathedral
name: Catedral de Jaen
```

Recommended objective naming direction:

```txt
id: objective-jaen-cathedral-<stable-suffix>
```

These are only technical naming directions. Final content rows must still follow user-confirmed non-technical inputs.

## Seed Strategy Guidance

Preferred option:

### Seed as a numbered SQL migration

Use a new SQL file after the current migration sequence.

Pros:

- keeps content history explicit
- matches the current D1 workflow
- is easy to replay in local and remote environments

Cons:

- makes the POI/objective baseline part of versioned product history

### Recommended MVP Choice

Prefer a numbered SQL migration unless a strong reason appears to split content seeding into a separate path.

## Naming Guidance

Example direction only:

```txt
0005_seed_jaen_cathedral_poi.sql
```

If objectives are seeded together with the POI, the file name may reflect both concerns as long as it stays short and explicit.

The exact migration number must follow the real sequence present in `apps/api/migrations/`.

## Data Integrity Guidance

The inserted records should:

- use stable text IDs
- use stable slugs
- reference `route-jaen-echoes-of-stone`
- use ISO datetime strings
- respect the `draft/published/archived` status model
- respect sibling ordering constraints already introduced in earlier migrations

Do not rely on random IDs for this MVP baseline seed.

## Expected Result

At the end of this task:

- D1 contains one POI for `Catedral de Jaen`
- that POI belongs to `Jaen: Echoes of Stone`
- D1 contains the first visual objective rows for that POI
- no `Banos Arabes` data has been inserted yet
- the seed path is documented and repeatable

## Acceptance Criteria

- the repository contains a clear, versioned mechanism for seeding `Catedral de Jaen`
- one POI row is inserted into `pois`
- the POI references `route-jaen-echoes-of-stone` correctly
- one or more user-confirmed objective rows are inserted into `visual_objectives`
- stable IDs and slugs are used
- objective ordering is explicit
- no `Banos Arabes` data is inserted
- no user, progress, analytics, or demo data is inserted
- existing build and test flows still pass

## Out of Scope

Do not include in this task:

- the `Banos Arabes` POI
- second-route or second-destination data
- persistence for progress
- authentication
- admin editing flows
- read endpoints
- mobile UI work
- AI behavior

## Definition of Done

This EVO is complete when the repository contains a stable seed for `Catedral de Jaen` and its first explicitly confirmed visual objectives, those rows apply correctly to local and remote D1, and the current backend foundation remains green.

## Suggested Implementation Order

1. confirm the Catedral POI business inputs
2. confirm the first objective business inputs
3. create the next numbered seed migration
4. extend the migration test coverage
5. apply the migration locally
6. apply the migration remotely
7. run test and build validation

## Backlog Note

This EVO should establish the first real POI slice for the MVP route.

`EVO-0073` should then add `Banos Arabes` as the second POI slice without reopening the Catedral seed or broadening this task into a full route dump.
